# Copy to deploy/deploy.env (gitignored) or export before deploy.

DEPLOY_HOST=121.41.4.126
DEPLOY_USER=root
DEPLOY_PATH=/yida/dingtalk-bots

# Password: prefer SSH key. If needed:
# DEPLOY_SSH_PASS=...

# Docker build proxy on this server (sing-box HTTP on :20171)
BUILD_HTTP_PROXY=http://172.17.0.1:20171
BUILD_HTTPS_PROXY=http://172.17.0.1:20171
BUILD_NO_PROXY=localhost,127.0.0.1
